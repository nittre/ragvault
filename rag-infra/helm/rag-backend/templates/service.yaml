apiVersion: v1
kind: Service
metadata:
  name: {{ include "rag-backend.fullname" . }}
  labels:
    {{- include "rag-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rag-backend.selectorLabels" . | nindent 4 }}
